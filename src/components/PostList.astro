---
const posts = await Astro.glob("../pages/blog/posts/*.md");
---

<div>
	<ul class="postList">
		{
			posts.map((post) => (
				<li>
					<a href={post.url}>{post.frontmatter.title}</a>
				</li>
			))
		}
	</ul>
</div>

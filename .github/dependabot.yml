version: 2
updates:
    - package-ecosystem: "github-actions"
      directory: "/"
      schedule:
          interval: "weekly"
      commit-message:
          prefix: "deps(actions)"

    - package-ecosystem: "npm"
      directory: "/"
      schedule:
          interval: "weekly"
      commit-message: "deps(node)"
